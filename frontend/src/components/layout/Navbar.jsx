// This module mirrors the documented `components/layout` path from the
// project structure. The single source of truth for the Navbar lives in
// `src/layout/Navbar.jsx` and is re-exported here to avoid duplicated logic.
export { default } from "../../layout/Navbar";
