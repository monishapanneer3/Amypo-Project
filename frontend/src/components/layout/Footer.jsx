// This module mirrors the documented `components/layout` path from the
// project structure. The single source of truth for the Footer lives in
// `src/layout/Footer.jsx` and is re-exported here to avoid duplicated logic.
export { default } from "../../layout/Footer";
